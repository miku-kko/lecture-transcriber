[project]
name = "lecture-transcriber"
version = "0.1.0"
description = "Real-time Polish lecture transcription with AI correction"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
